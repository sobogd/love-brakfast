export enum ELangs {
  EN = "en",
  RU = "ru",
  TR = "tr",
  UA = "ua",
}

export enum EMenuItemTypes {
  LINK = "link",
  BUTTON = "button",
}

export enum EMenuTypes {
  GENERAL = "general",
  LANG = "lang",
}
